name: 'Extract Pull Request Metadata'
description: 'Extract Pull Request Metadata
author: 'Scacap Hackathon'
branding:
  icon: 'check'
  color: 'blue'
inputs:
  repo-token:
    description: 'Token for the repo. Can be passed in using {{ secrets.GITHUB_TOKEN }}'
    required: true
runs:
  using: 'node12'
  main: 'lib/index.js'

